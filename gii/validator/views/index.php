<h1>Generate annotations for validators</h1>
<p>This will generate annotations for existing validator classes.
	Specialized annotations for validators are not required, however it is
	somewhat more convenient to write ie. <code>@RequiredValidator</code> than
	<code>@Validator('required')</code>, or not?
</p>